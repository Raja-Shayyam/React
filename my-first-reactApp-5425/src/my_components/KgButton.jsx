export function MyButton() // its also a type to export 
{
  return <button>this is my first button on react by Kgcodings </button>;
}

// export default MyButton; # this if multiple and you need to export some of them then at last you declate them ..;